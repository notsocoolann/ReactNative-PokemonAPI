export type PokemonList ={
    name: string;
    url: string; 
}

export type PokemonDetails ={
    base_experience: string;
    height: string;
    held_items: string;
    id: string;
    weight: string; 
}